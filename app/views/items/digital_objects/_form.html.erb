<%= form_with(model: items_digital_object, local: true) do |form| %>
  <% if items_digital_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(items_digital_object.errors.count, "error") %> prohibited this items_digital_object from being saved:</h2>

      <ul>
        <% items_digital_object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
